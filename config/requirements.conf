# Apps that omacosy's settings rely on: app bundle name, minimum version,
# Homebrew cask. install.sh and omacosy-update check them. Raise a minimum
# when omacosy starts writing a setting that only a newer version reads.
# OmniWM 0.7.0: theme-set writes darkColor, gradient and glow border tables.
OmniWM  0.7.0  omniwm
