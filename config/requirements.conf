# Apps that omacchiato's settings rely on: app bundle name, minimum version,
# Homebrew cask. install.sh and omacchiato-update check them. Raise a minimum
# when omacchiato starts writing a setting that only a newer version reads.
# OmniWM 0.7.0: theme-set writes darkColor, gradient and glow border tables.
OmniWM  0.7.0  omniwm
